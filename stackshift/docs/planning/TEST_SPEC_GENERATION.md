# Test Specification Generation (Gear 7)

**Version:** 1.0 (Simplified)
**Status:** Proposed
**Date:** 2025-12-15
**Implementation Time:** 1 week

---

## Problem Statement

**Current State:**
- StackShift generates `test-documentation.md` with high-level test descriptions
- Developers manually convert these to test code (50-65 hours)
- LLMs (Claude/Copilot) can generate code, but need better input format

**Current Output Example:**
```markdown
## Testing Requirements

**Test Coverage Goals:**
- Unit Tests: 80%

**Test Scenarios:**
1. User should be able to search incentives by make/model
2. Filter panel should update results dynamically
```
❌ **Too vague** for AI code generation

**Desired Output Example:**
```markdown
## Unit Test: Search by Make/Model

**Test 1: Successful search**
Given: User enters make="Honda", model="Accord"
When: searchIncentives() is called
Then: Returns array of Honda Accord incentives

Test Data:
```javascript
{ make: "Honda", model: "Accord", year: 2024 }
```

Expected Result:
```javascript
expect(result).toHaveLength(5)
expect(result[0].make).toBe("Honda")
```
```
✅ **AI-ready** - Claude/Copilot can generate working test code

---

## Solution: Enhanced Test Specifications

**Goal:** Generate AI-friendly test specifications that developers can easily convert to code.

**Approach:**
1. StackShift extracts business logic (already does this via LLM)
2. StackShift formats output as **Given-When-Then** test specs
3. Developer copies spec into Claude/Copilot
4. AI generates test code in their framework
5. Developer reviews and runs tests

**Key Insight:** StackShift is already LLM-based. Keep it that way.

---

## Output Format Specification

### New File: `test-specifications.md`

**Structure:**
```markdown
# Test Specifications: [Widget Name]

> Generated by StackShift Gear 7
> For use with AI-assisted test generation (Claude, Copilot)

## Summary
- **Total test scenarios:** 156
- **Estimated coverage:** 87%
- **Test categories:** Unit (89), Integration (35), Component (32)

---

## Unit Tests

### Category: Disclaimer Display Logic

#### Test Suite: State Disclaimer Length Rules

**Test 1: Display full text for short disclaimers**

```yaml
ID: TEST-001
Description: Disclaimers ≤200 characters should display full text
Category: Business Logic
Priority: High

Given:
  - disclaimer.type = "state"
  - disclaimer.text.length ≤ 200

When:
  - shouldShowFullText(disclaimer) is called

Then:
  - Returns: true

Test Data:
  disclaimer:
    type: "state"
    text: "This disclaimer is under 200 characters."

Expected Assertion:
  expect(result).toBe(true)

Source: business-rules.md > State Disclaimer Display Logic
```

**Test 2: Display link for long disclaimers**

```yaml
ID: TEST-002
Description: Disclaimers >200 characters should display link only
Category: Business Logic
Priority: High

Given:
  - disclaimer.type = "state"
  - disclaimer.text.length > 200

When:
  - shouldShowFullText(disclaimer) is called

Then:
  - Returns: false

Test Data:
  disclaimer:
    type: "state"
    text: "{{ repeat 'x' 201 times }}"  # 201 characters

Expected Assertion:
  expect(result).toBe(false)

Edge Cases:
  - Exactly 200 characters (boundary test)
  - Exactly 201 characters (just over boundary)
  - Empty string (0 characters)
  - Null/undefined input

Source: business-rules.md > State Disclaimer Display Logic
```

---

### Category: Vendor-Specific Logic

#### Test Suite: Honda Data Source Selection

**Test 1: Use gvIncentiveTool for Honda vehicles**

```yaml
ID: TEST-003
Description: Honda vehicles should use alternate data source
Category: Vendor-Specific
Priority: High
JIRA: JIRA-26353

Given:
  - vehicle.make = "Honda"

When:
  - getIncentiveDataSource(vehicle) is called

Then:
  - Returns: { source: "gvIncentiveTool", fallback: "overviewIncentives" }

Test Data:
  vehicle:
    make: "Honda"
    model: "Accord"
    year: 2024

Expected Assertions:
  expect(result.source).toBe("gvIncentiveTool")
  expect(result.fallback).toBe("overviewIncentives")

Related Tests:
  - TEST-004: Non-Honda vehicles use standard source
  - TEST-005: Honda Fit Group 1 disclaimer

Source: business-rules.md > Vendor-Specific Logic: Honda
```

**Test 2: Non-Honda vehicles use standard source**

```yaml
ID: TEST-004
Description: Non-Honda vehicles use overviewIncentives
Category: Vendor-Specific
Priority: Medium

Given:
  - vehicle.make != "Honda"

When:
  - getIncentiveDataSource(vehicle) is called

Then:
  - Returns: { source: "overviewIncentives" }

Test Data (multiple):
  - { make: "Toyota", model: "Camry", year: 2024 }
  - { make: "Ford", model: "F-150", year: 2024 }
  - { make: "Tesla", model: "Model 3", year: 2024 }

Expected Assertion:
  expect(result.source).toBe("overviewIncentives")

Source: business-rules.md > Vendor-Specific Logic: Honda
```

---

## Integration Tests

### Category: DVS API Integration

#### Test Suite: Fetch Incentives Success Scenarios

**Test 1: Fetch incentives for Honda Accord**

```yaml
ID: TEST-INT-001
Description: Successfully fetch incentives from DVS API
Category: Integration
Priority: High

Given:
  - DVS API is available and responding
  - Mock response contains incentive data

When:
  - fetchIncentives({ make: "Honda", model: "Accord", year: 2024 })

Then:
  - Returns: Array of incentive objects
  - Calls DVS API with correct parameters

Mock Setup:
```javascript
mockDVS.fetchIncentives.mockResolvedValue({
  incentives: [
    { id: "HONDA_LEASE_1", make: "Honda", model: "Accord", type: "LEASE" }
  ],
  meta: { total: 1, timestamp: "2024-01-01T00:00:00Z" }
})
```

Test Data:
  params:
    make: "Honda"
    model: "Accord"
    year: 2024
    accountId: "acct-123"

Expected Assertions:
```javascript
expect(result.incentives).toHaveLength(1)
expect(result.incentives[0].make).toBe("Honda")
expect(mockDVS.fetchIncentives).toHaveBeenCalledWith({
  make: "Honda",
  model: "Accord",
  year: 2024,
  accountId: "acct-123"
})
```

Source: data-flow.md > DVS API Integration Point
```

#### Test Suite: Error Handling

**Test 1: Handle 404 - No incentives found**

```yaml
ID: TEST-INT-002
Description: Gracefully handle 404 errors from DVS API
Category: Integration - Error
Priority: High

Given:
  - DVS API returns 404 Not Found
  - Vehicle make/model has no incentives

When:
  - fetchIncentives({ make: "Oldsmobile", model: "Cutlass" })

Then:
  - Throws: Error with status 404
  - Error message: "Not Found"

Mock Setup:
```javascript
const error = new Error("Not Found")
error.statusCode = 404
mockDVS.fetchIncentives.mockRejectedValue(error)
```

Expected Assertion:
```javascript
await expect(fetchIncentives(params)).rejects.toThrow("Not Found")
```

Source: data-flow.md > DVS API Error Scenarios
```

---

## Component Tests (React)

### Category: IncentiveDetail Component

#### Test Suite: Conditional Rendering by Type

**Test 1: Display monthly payment for LEASE incentives**

```yaml
ID: TEST-COMP-001
Description: LEASE incentives show monthly payment
Category: Component - React
Priority: High
Framework: React Testing Library

Given:
  - incentive.type = "LEASE"
  - incentive.monthlyPayment = 299

When:
  - <IncentiveDetail incentive={incentive} /> is rendered

Then:
  - Page displays: "$299/month"

Test Data:
```javascript
const incentive = {
  id: "HONDA_LEASE_1",
  type: "LEASE",
  make: "Honda",
  model: "Accord",
  monthlyPayment: 299,
  terms: { duration: 36, milesPerYear: 12000 }
}
```

Expected Assertions:
```javascript
render(<IncentiveDetail incentive={incentive} />)
expect(screen.getByText("$299/month")).toBeInTheDocument()
expect(screen.queryByText(/Cash Back/)).not.toBeInTheDocument()
```

Source: component-catalog.md > IncentiveDetail
```

**Test 2: Display cash back amount for CASH_BACK incentives**

```yaml
ID: TEST-COMP-002
Description: CASH_BACK incentives show cash back amount
Category: Component - React
Priority: High
Framework: React Testing Library

Given:
  - incentive.type = "CASH_BACK"
  - incentive.cashBackAmount = 2500

When:
  - <IncentiveDetail incentive={incentive} /> is rendered

Then:
  - Page displays: "$2,500 Cash Back"

Test Data:
```javascript
const incentive = {
  id: "TOYOTA_CASHBACK_1",
  type: "CASH_BACK",
  make: "Toyota",
  model: "Camry",
  cashBackAmount: 2500
}
```

Expected Assertions:
```javascript
render(<IncentiveDetail incentive={incentive} />)
expect(screen.getByText("$2,500 Cash Back")).toBeInTheDocument()
expect(screen.queryByText(/month/)).not.toBeInTheDocument()
```

Source: component-catalog.md > IncentiveDetail
```

---

## Mock Data Requirements

### Mock: DVS API Client

```javascript
// Suggested mock structure for tests/mocks/dvs.mock.js

export const mockDVSClient = {
  fetchIncentives: jest.fn((params) => {
    // Return incentives based on make
    const incentivesByMake = {
      Honda: [{ id: "HONDA_LEASE_1", make: "Honda", type: "LEASE" }],
      Toyota: [{ id: "TOYOTA_CASH_1", make: "Toyota", type: "CASH_BACK" }]
    }
    return Promise.resolve({
      incentives: incentivesByMake[params.make] || [],
      meta: { total: incentivesByMake[params.make]?.length || 0 }
    })
  }),

  fetchVehicleTrims: jest.fn((vehicleId) => {
    const trimsByVehicle = {
      "honda-accord-2024": ["LX", "EX", "EX-L", "Touring"],
      "toyota-camry-2024": ["LE", "SE", "XLE", "XSE"]
    }
    return Promise.resolve(trimsByVehicle[vehicleId] || [])
  })
}

jest.mock("@/services/dvs", () => mockDVSClient)
```

**Source:** data-flow.md > DVS Integration Point

---

### Fixture: Honda Lease Incentive

```javascript
// Suggested fixture for tests/fixtures/incentives/honda-lease.json

{
  "id": "HONDA_LEASE_2024_ACCORD",
  "make": "Honda",
  "model": "Accord",
  "year": 2024,
  "type": "LEASE",
  "title": "Honda Accord Lease Offer",
  "effectiveDate": "2024-01-01",
  "expirationDate": "2024-12-31",
  "terms": {
    "monthlyPayment": 299,
    "duration": 36,
    "milesPerYear": 12000,
    "dueAtSigning": 2999
  },
  "disclaimers": [
    {
      "type": "general",
      "text": "Offer valid for qualified buyers with approved credit"
    },
    {
      "type": "state",
      "text": "See dealer for complete details."
    }
  ],
  "applicableTrims": ["EX", "EX-L", "Touring"]
}
```

**Source:** data-flow.md > Incentive Data Structure

---

## How to Use These Specifications

### Step 1: Copy Test Specification

Select a test specification from this document (e.g., TEST-001).

### Step 2: Generate Test Code with AI

**Prompt for Claude/Copilot:**
```
Generate Jest test code from this specification:

[paste TEST-001 here]

Use:
- Jest as test framework
- React Testing Library for components
- Follow Arrange-Act-Assert pattern
- Include all assertions from spec
```

### Step 3: Review Generated Code

**AI will generate something like:**
```javascript
// tests/unit/business-logic/disclaimer-logic.test.js
describe('State Disclaimer Display Logic', () => {
  it('should display full text for disclaimers ≤200 characters', () => {
    // Arrange
    const disclaimer = {
      type: 'state',
      text: 'This disclaimer is under 200 characters.'
    };

    // Act
    const result = shouldShowFullText(disclaimer);

    // Assert
    expect(result).toBe(true);
  });

  it('should display link for disclaimers >200 characters', () => {
    // Arrange
    const disclaimer = {
      type: 'state',
      text: 'x'.repeat(201)
    };

    // Act
    const result = shouldShowFullText(disclaimer);

    // Assert
    expect(result).toBe(false);
  });
});
```

### Step 4: Add Edge Cases

Review specification's "Edge Cases" section and add boundary tests:
```javascript
it('should handle exactly 200 characters (boundary)', () => {
  const disclaimer = { type: 'state', text: 'x'.repeat(200) };
  expect(shouldShowFullText(disclaimer)).toBe(true);
});

it('should handle exactly 201 characters (just over)', () => {
  const disclaimer = { type: 'state', text: 'x'.repeat(201) };
  expect(shouldShowFullText(disclaimer)).toBe(false);
});
```

### Step 5: Run Tests

```bash
npm test
```

### Batch Generation (Optional)

**Prompt for generating entire test suite:**
```
Generate a complete Jest test suite from all unit test specifications in this document.

Requirements:
- Use Jest + React Testing Library
- Organize tests by category (business-logic/, vendor-specific/, etc.)
- Include all test data and assertions from specs
- Add setup/teardown as needed
- Follow project conventions in existing tests

[paste entire test-specifications.md]
```

---

## Coverage Expectations

**With these specifications:**
- ✅ **85-90% coverage** achievable via AI generation
- ✅ **All major business rules** covered
- ✅ **Integration points** mocked and tested
- ✅ **Component rendering** validated
- ⚠️ **Boundary cases** may need manual review
- ⚠️ **Complex edge cases** require developer judgment

**Comparison:**
- Manual test writing: 50-65 hours → 80-90% coverage
- AI from these specs: 10-15 hours → 85-90% coverage
- **Time savings:** 40-50 hours (~1 week)

---

## Implementation Plan

### Week 1: Template Creation (5 days)

**Day 1-2: Design Specification Format**
- Create template structure (Given-When-Then YAML)
- Define test categories (unit, integration, component)
- Design mock/fixture templates

**Day 3-4: Prompt Engineering**
- Update Gear 2 (reverse-engineer) prompts
- Add test specification generation to output
- Test with incentives-search widget

**Day 5: Documentation & Testing**
- Write usage guide
- Test AI generation with Claude/Copilot
- Refine format based on results

### Week 2: Integration (optional)

**If needed:**
- Add `/stackshift.generate-tests` command
- Update cruise-control workflow
- Add to Gear 7 slot

**Alternatively:**
- Just include in existing Gear 2 output
- No new command needed

---

## Success Criteria

**Minimum Viable:**
- ✅ Generates `test-specifications.md` with 80+ test scenarios
- ✅ Claude/Copilot can generate working tests from specs
- ✅ Achieves 85%+ coverage when implemented
- ✅ Reduces manual test creation from 50-65h to 10-15h

**Quality Goals:**
- ✅ All business rules have test specifications
- ✅ All vendor-specific logic covered
- ✅ All integration points have success + error tests
- ✅ Component tests cover conditional rendering
- ✅ Mock data structures match actual APIs

**Developer Experience:**
- ✅ Copy-paste workflow is smooth
- ✅ AI-generated code passes lint
- ✅ Tests run successfully without modification
- ✅ Developers can easily add edge cases

---

## Example Output Size

**For incentives-search widget (974 LOC):**

**test-specifications.md:**
- Unit tests: 89 specifications
- Integration tests: 35 specifications
- Component tests: 32 specifications
- **Total:** 156 test specifications
- **File size:** ~8,000 lines (highly structured)
- **Reading time:** 20 minutes (for developer review)
- **AI processing:** 5-10 minutes (batch generation)

---

## Configuration

**Optional config in `.stackshift/config.json`:**

```json
{
  "testSpecifications": {
    "enabled": true,
    "format": "yaml",  // or "markdown", "json"
    "includeEdgeCases": true,
    "includeMockData": true,
    "includeFixtures": true,
    "testFramework": "jest",  // for framework-specific hints
    "aiOptimized": true  // format for LLM consumption
  }
}
```

---

## Comparison to Original Proposal

| Aspect | Original (AST-based) | Simplified (LLM-based) |
|--------|---------------------|------------------------|
| **Implementation** | 11 weeks | 1 week |
| **Architecture** | Custom parsers + templates | Enhanced LLM output |
| **Complexity** | High (7 components) | Low (1 template) |
| **Coverage** | 95-100% | 85-90% |
| **Maintenance** | Complex | Simple |
| **Framework Support** | Locked to Jest initially | Any framework |
| **Velocity Support** | Would need parser | Works today (LLM) |
| **Consistency** | Inconsistent with StackShift | Consistent with StackShift |
| **Developer Control** | Less (auto-generated) | More (AI-assisted) |

---

## Future Enhancements (Optional)

**Phase 2: AI Integration (if desired)**
```bash
/stackshift.generate-tests --auto
```
- Call Claude API to generate code automatically
- Validate and write `.test.js` files
- Requires API key, costs $5-10 per widget
- Still keeps human review loop

**Phase 3: Coverage Analysis**
- Compare generated tests to source LOC
- Identify coverage gaps
- Suggest additional tests

**Phase 4: Test Execution**
- Run generated tests
- Report failures
- Iterate on specs

**But start with Phase 1:** Just generate better specifications.

---

## Conclusion

**Simple is Better:**
- ✅ 1 week instead of 11 weeks
- ✅ Consistent with StackShift's LLM architecture
- ✅ Flexible (works with any test framework)
- ✅ Developer maintains control
- ✅ 85-90% coverage is good enough

**Next Step:** Implement enhanced `test-specifications.md` template in Gear 2.

---

**Document Status:** Ready for implementation
**Approval Required:** Yes
**Implementation Start:** After approval
